# BRANCHPOINT

Agents get branches before they get permissions.

BRANCHPOINT is a counterfactual execution and adversarial verification layer for autonomous AI agents.

## Components

- `backend/` — Python API foundation
- `frontend/` — planned for a later phase
